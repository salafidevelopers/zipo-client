const ShareIcon = ({ src }) => {
  return (
    <img
      src={src}
      width={25}
      style={{ marginLeft: "3px", display: "inline" }}
      alt="share"
    />
  );
};

export default ShareIcon;
